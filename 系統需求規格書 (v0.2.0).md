# **幫你算兌 Money Snap \- 系統需求規格書 (Requirements Specification)**

## **1\. 專案背景與目標**

在現發現金薪資（如承攬人員、兼職人員、工作室發薪、活動發放等）的作業情境中，財務人員需將數十至數百筆不等的發放金額拆解為不同面額（如 1000元, 500元, 100元...），並於前往銀行前統整換鈔數量。此過程若以人工或 Excel 複雜公式計算，極易出錯且耗時。

「幫你算兌 Money Snap」旨在打造一款**輕量、零後端伺服器依賴、極致資安隔離**的純前端瀏覽器兌幣計算工具。系統可自動解析貼入的資料，產出銀行領款總量與個人薪資袋面額卡片，降低財務人員的操作門檻與出錯率。

## **2\. 使用者角色與痛點**

### **2.1 主要使用者角色 (Target Persona)**

* **財務人員 / 人事行政**：每月或每期需手動包現金薪資袋者。  
* **工作室負責人 / 市集主辦**：需快速計算找零或現場派工現領金額者。

### **2.2 使用者痛點**

1. **換鈔試算繁瑣**：前往銀行前，需人工計算所需鈔票與硬幣數量。  
2. **複製貼上格式混亂**：來源資料常帶有貨幣符號（$）、千分位（,）或缺少姓名。  
3. **數據資安隱私風險**：將員工薪資姓名上傳至第三方雲端伺服器存在洩密疑慮。  
4. **包薪資袋核對困難**：缺乏清晰的「個人發放面額小卡」作為裝袋依據。

## **3\. 功能性需求 (Functional Requirements)**

### **FR-01：彈性資料輸入模式 (Input Modes)**

* **FR-01.1 姓名開關 Switch/Checkbox**：系統提供「貼上資料包含姓名欄位」切換開關。  
  * **勾選狀態（預設）**：解析 姓名, 金額 格式（分隔符支援逗號、空格、Tab）。  
  * **未勾選狀態**：解析 純金額 格式，每行一筆，自動補上預設識別碼（如 項目 \#1）。  
* **FR-01.2 金額格式容錯**：輸入金額自動去除非數字字元（保留正整數），自動處理 $45,800、32,000 等常見格式。  
* **FR-01.3 動態 Placeholder**：根據開關狀態切換輸入框中的提示文字範例。

### **FR-02：自動化跨項目加總與面額拆解 (Calculation Engine)**

* **FR-02.1 同名加總**：在「包含姓名」模式下，若輸入多筆相同姓名（如同人多筆兼職），系統自動合併加總。  
* **FR-02.2 預設貪婪面額算法**：預設採用國幣標準面額（1000, 500, 100, 50, 10, 5, 1），由大至小優先拆解。  
* **FR-02.3 雙重對帳驗證**：系統必須自動比對「輸入總額」與「拆解後面額加總總額」，保證 ![][image1]。

### **FR-03：銀行領款總表輸出 (Bank Summary)**

* 輸出本次發放總金額、總筆數。  
* 輸出所有鈔票（張）與硬幣（個）的加總總量。  
* 提供「一鍵複製銀行領款單」功能，將統計數據轉為文字格式存入剪貼簿。

### **FR-04：個人發放面額卡片 (Individual Slip Cards)**

* 獨立呈現每筆項目（或個人）應領金額及所需面額組合。  
* 無姓名模式下，姓名處顯示 項目 \#1, 項目 \#2。

### **FR-05：輸出與匯出 (Exporting)**

* **圖片匯出**：提供「匯出明細圖片 (PNG)」功能，將個人明細區塊轉為圖片，供印製或存查。

### **FR-06：狀態記憶與離線使用 (Persistence & PWA)**

* **FR-06.1 狀態記憶**：使用 localStorage 自動記憶輸入內容與 Checkbox 開關狀態。  
* **FR-06.2 離線運行 (PWA)**：透過 Service Worker 快取核心資源，斷網環境下可正常運行。

## **4\. 非功能性需求 (Non-Functional Requirements)**

### **NFR-01：資安與數據隱私 (Privacy & Security Constraint)**

* **零伺服器傳輸**：100% 瀏覽器本地運算 (Client-side Execution)，禁止發送帶有薪資內容或姓名之網路請求。  
* **GA 追蹤安全限制**：埋設之 Google Analytics 追蹤事件僅限記錄按鈕點擊動作（如 click\_calculate），嚴禁帶入任何使用者輸入之內容。

### **NFR-02：效能需求 (Performance)**

* 於一般桌機與行動裝置，輸入 200 筆資料內之解析與拆解計算時間須小於 100ms。

### **NFR-03：跨平台與回應式設計 (RWD)**

* 支援桌面端（Web）與行動端（Mobile Browser / PWA 獨立視窗）。

## **5\. 未來擴充性規劃 (Roadmap Preview)**

* **Phase 2**：CSV 檔案匯入/匯出功能（與 Checkbox 模式連動）。  
* **Phase 3**：自訂面額與多國貨幣預設值（Preset）切換。
