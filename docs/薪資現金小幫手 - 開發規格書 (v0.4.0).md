1. 專案背景與財務實務情境 (Financial Context & Pain Points)

身為負責每月發放現金薪資與承攬報酬的財務人員，在去銀行換鈔以及進行薪資袋裝封時，經常面臨以下實務挑戰：

1. 預設體驗極簡化 (Default Streamlined UX)：

	• 絕大多數月份均使用台灣標準流通面額（1000, 500, 100, 50, 10, 5, 1）。預設不開啟自訂選項，僅以純文字提示「已啟用預設面額」，維持介面簡潔。

2. 銀行特定面額短缺（開關啟用情境）：

	• 臨櫃換鈔時，若銀行通知「今日 50 元硬幣庫存不足」或「2000 元/200 元不開放兌換」，財務人員可勾選 「啟用自訂面額設定」，取消勾選短缺面額，系統即時自動改用小面額遞補拆解。

3. 分區全選便捷控制 (Sectional Select All)：

	• 捨棄複雜的快捷範本，直接於「紙鈔面額」與「硬幣面額」標題旁設置 「全選」 按鈕，方便快速重置或批次勾選。

4. 偏好設定記憶 (localStorage Persistence)：

	• 提供「記住我的面額偏好」選項，可將自訂狀態（含開關與勾選面額）儲存於瀏覽器本地，避免每月重複設定。

2. 技術架構與資安規範 (Tech Stack & Security Baseline)

• 前端核心：Pure Vanilla JavaScript (ES6+), HTML5, CSS3

• UI 樣式：Tailwind CSS (CDN / Compiled Class)

• 資安基線 (Strict Security Principle)：

	• 100% Client-side 沙盒運算：所有面額設定與試算邏輯均於用戶瀏覽器內完成。

	• 零外部請求：嚴禁發送任何 API 請求或上傳薪資數據至雲端伺服器。

• 本地儲存：localStorage (Key: money_snap_denom_config_v4)

• 成效追蹤：GA4 匿名動作監聽（只紀錄按鈕動作，嚴禁包含金額與人名數據）。

3. 資料結構設計 (TypeScript Interfaces)

[typescript]
/**
 * 面額型態 (單位：元)
 */
export type Denomination = number;

/**
 * 預設流通面額清單 (依面額由大至小排序)
 * 預設排除罕見的 2000 元與 200 元
 */
export const DEFAULT_ACTIVE_DENOMINATIONS: Denomination[] = [
  1000, 500, 100, 50, 10, 5, 1
];

/**
 * 全套支援面額清單 (供自訂選單勾選使用)
 */
export const ALL_SUPPORTED_DENOMINATIONS: {
  paper: Denomination[];
  coin: Denomination[];
} = {
  paper: [2000, 1000, 500, 200, 100],
  coin: [50, 20, 10, 5, 1]
};

/**
 * 自訂面額組態設定介面
 */
export interface DenominationConfig {
  /** 是否啟用自訂面額 (預設 false) */
  enabled: boolean;
  /** 目前勾選啟用的面額清單 (必須保持由大至小排序) */
  activeDenominations: Denomination[];
  /** 是否儲存為使用者預設偏好 (儲存至 localStorage) */
  saveAsDefault: boolean;
}

/**
 * 完整應用程式 LocalStorage 記憶結構 (v0.4.0)
 */
export interface AppStorageV4 {
  rawInputText: string;
  hasNameHeader: boolean;
  denomConfig: DenominationConfig;
}


4. 核心演算法：彈性貪婪面額拆解 (Adaptable Greedy Breakdown)

4.1 演算法實作邏輯

當用戶未開啟自訂面額時，系統強制採用預設面額陣列；開啟時則依據 denomConfig.activeDenominations 進行動態貪婪拆解。

[javascript]
/**
 * 彈性貪婪面額拆解演算法
 * @param {number} amount - 待拆解金額 (需為正整數)
 * @param {DenominationConfig} config - 面額設定物件
 * @returns {Record<number, number>} 各面額張/個數對照表
 */
function calculateBreakdownV4(amount, config) {
  let remaining = amount;
  const breakdown = {};

  // 1. 判斷使用預設面額或自訂面額
  const rawDenoms = config && config.enabled
    ? config.activeDenominations
    : DEFAULT_ACTIVE_DENOMINATIONS;

  // 2. 安全防呆：強制將啟用面額進行「由大至小」降冪排序
  const sortedDenoms = [...rawDenoms].sort((a, b) => b - a);

  // 3. 防呆驗證：若未選擇任何面額，拋出錯誤提示
  if (sortedDenoms.length === 0) {
    throw new Error('請至少選擇一種有效面額進行計算！');
  }

  // 4. 貪婪演算法拆解
  sortedDenoms.forEach(d => {
    const count = Math.floor(remaining / d);
    breakdown[d] = count;
    remaining %= d;
  });

  // 5. 殘額檢查 (若自訂面額未包含 1 元導致剩餘金額無法除盡)
  if (remaining > 0) {
    console.warn(`[Money Snap] 金額 $${amount} 在當前設定下殘留 $${remaining} 無法完全拆解。建議勾選 1 元面額。`);
  }

  return breakdown;
}


4.2 雙重對帳邏輯整合 (Double-Entry Verification)

不論面額如何設定，系統必須維持 ‭$S_{input} == S_{breakdown}$‬ 之資安與財務鐵則：

[javascript]
/**
 * v0.4.0 彈性對帳驗證
 * @param {Array<{name: string, amount: number}>} parsedItems 
 * @param {Record<number, number>} bankTotals 
 */
function verifyDoubleEntryFlexible(parsedItems, bankTotals) {
  const sumInput = parsedItems.reduce((acc, cur) => acc + cur.amount, 0);
  
  let sumBreakdown = 0;
  Object.entries(bankTotals).forEach(([denom, count]) => {
    sumBreakdown += Number(denom) * count;
  });

  if (sumInput !== sumBreakdown) {
    throw new Error(`【財務嚴重警告】輸入總額 ($${sumInput}) 與面額拆解總金額 ($${sumBreakdown}) 不符！請確認是否漏選 1 元面額。`);
  }
  return true;
}


5. UI 視覺線稿與互動規範 (UI Wireframe & Interaction)

5.1 純文字線稿 (ASCII Wireframe)

情境 A：未勾選「啟用自訂面額設定」（預設狀態）

[text]
================================================================================
幫你算兌 Money Snap - 兌幣計算機 (v0.4.0)
================================================================================

[ 區塊一：資料輸入與組態 ]
[☑] 貼上資料包含姓名欄位 (取消勾選即可貼上純金額)

+--------------------------------------------------------------------------+
| 請輸入或貼上資料 (支援 Excel 直接複製貼上)：                                |
|                                                                          |
+--------------------------------------------------------------------------+

[ ] 啟用自訂面額設定 (未勾選之面額將不參與拆解)
已啟用預設面額：1000, 500, 100, 50, 10, 5, 1

[ 🗑️ 清除所有資料 ]                            [ 🚀 計算與統計 ]

================================================================================


情境 B：勾選「啟用自訂面額設定」（展開自訂面板）

[text]
================================================================================
幫你算兌 Money Snap - 兌幣計算機 (v0.4.0)
================================================================================

[ 區塊一：資料輸入與組態 ]
[☑] 貼上資料包含姓名欄位 (取消勾選即可貼上純金額)

+--------------------------------------------------------------------------+
| 請輸入或貼上資料 (支援 Excel 直接複製貼上)：                                |
|                                                                          |
+--------------------------------------------------------------------------+

[☑] 啟用自訂面額設定 (未勾選之面額將不參與拆解)

┌──────────────────────────────────────────────────────────────────────────┐
│ 紙鈔面額 [ 全選 ]：                                                      │
│ [ ] $2000   [☑] $1000   [☑] $500   [ ] $200   [☑] $100                    │
│                                                                          │
│ 硬幣面額 [ 全選 ]：                                                      │
│ [☑] $50     [ ] $20     [☑] $10    [☑] $5     [☑] $1 (建議保留)           │
│                                                                          │
│ [☑] 記住我的面額偏好 (儲存至此瀏覽器)                                    │
└──────────────────────────────────────────────────────────────────────────┘

[ 🗑️ 清除所有資料 ]                            [ 🚀 計算與統計 ]

================================================================================


5.2 UI 控制項互動邏輯

1. 預設狀態 (Default State)：

	• 啟用自訂面額設定 Checkbox 預設為 未勾選 (false)。

	• 下方僅顯示淡灰色/純文字：已啟用預設面額：1000, 500, 100, 50, 10, 5, 1。

2. 勾選切換 (Switch Toggle Logic)：

	• 當使用者勾選 [☑] 啟用自訂面額設定 時，隱藏純文字提示，並顯示自訂面額面板（包含紙鈔/硬幣核選框與偏好記憶按鈕）。

	• 當使用者取消勾選 [ ] 啟用自訂面額設定 時，隱藏自訂面板，重新顯示純文字提示，並將計算邏輯還原為預設面額。

3. 「全選」按鈕功能 (Select All Toggles)：

	• 紙鈔全選：點擊後一鍵將 $2000, $1000, $500, $200, $100 全部勾選（若已全選則一鍵取消）。

	• 硬幣全選：點擊後一鍵將 $50, $20, $10, $5, $1 全部勾選（若已全選則一鍵取消）。

4. 防空選機制 (Minimum Guardrail)：

	• 若使用者在自訂模式下取消所有面額勾選，系統需於按下「計算與統計」時，跳出警告訊息：「請至少勾選一種面額（建議包含 1 元）以利計算！」。

5. 狀態持久化 (localStorage Persistence)：

	• 當勾選 記住我的面額偏好 時，隨即將 denomConfig 物件（包含 enabled 開關狀態與 activeDenominations）寫入 localStorage。

	• 頁面重新載入時自動讀取該組態並還原 UI 狀態。

6. GA4 事件埋點規範 (Analytics Spec)

事件名稱 (Event Name)	觸發時機 (Trigger)	帶入參數 (Params)	資安維護說明
toggle_custom_denom_switch	勾選或取消「啟用自訂面額設定」	enabled: boolean	僅紀錄開關狀態
click_select_all_paper	點擊紙鈔區塊的「全選」按鈕	action: 'select_all_paper'	零敏感數據
click_select_all_coin	點擊硬幣區塊的「全選」按鈕	action: 'select_all_coin'	零敏感數據
change_custom_denomination	勾選或取消特定面額	denom_value: number, active: boolean	僅紀錄面額數值
toggle_save_denom_preference	切換「記住我的偏好」勾選	enabled: boolean	僅紀錄開關狀態

7. 測試案例與驗收標準 (Test Cases & Acceptance Criteria)

測試案例 1：未開啟自訂面額（預設狀態驗證）

• 輸入資料：張三, 185

• 面額設定：[ ] 啟用自訂面額設定（未勾選，顯示純文字提示）。

• 預期結果：

	• 使用預設面額（1000, 500, 100, 50, 10, 5, 1）

	• 拆解結果：$100 元 ‭$\times 1$‬、$50 元 ‭$\times 1$‬、$10 元 ‭$\times 3$‬、$5 元 ‭$\times 1$‬

	• 總和 ‭$100 + 50 + 30 + 5 = 185$‬ 元，雙重對帳驗證成功。

測試案例 2：開啟自訂面額（銀行 50 元短缺情境）

• 輸入資料：張三, 185

• 面額設定：

    1. 勾選 [☑] 啟用自訂面額設定

    2. 取消勾選 $50 硬幣

• 預期結果：

	• 拆解結果：$100 元 ‭$\times 1$‬、$10 元 ‭$\times 8$‬、$5 元 ‭$\times 1$‬

	• 原 50 元自動退回改由 10 元組合，總金額 ‭$185$‬ 元完全吻合。

測試案例 3：紙鈔 / 硬幣全選按鈕測試

• 步驟：

    1. 勾選 [☑] 啟用自訂面額設定

    2. 點擊「紙鈔面額」旁邊的 [全選] 按鈕

• 預期結果：2000, 1000, 500, 200, 100 等 5 種紙鈔面額全部變為勾選狀態。

測試案例 4：localStorage 開關與面額偏好持久化驗證

• 步驟：

    1. 勾選 [☑] 啟用自訂面額設定 與 [☑] 記住我的面額偏好

    2. 取消勾選 $500 與 $50

    3. 重新整理網頁 (F5)

• 預期結果：網頁載入後，自訂面額開關維持開啟，且 $500 與 $50 保持未勾選狀態。